<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:bottom="@dimen/tag_stroke_width"
        android:left="@dimen/tag_stroke_width"
        android:right="@dimen/tag_stroke_width"
        android:top="@dimen/tag_stroke_width">

        <shape>

            <corners
                android:radius="@dimen/tag_corner_radius"/>
            <solid
                android:color="@color/list_tag_background"/>
        </shape>

    </item>

    <item>

        <shape>

            <corners
                android:radius="@dimen/tag_corner_radius"/>
            <stroke
                android:width="2dp"
                android:color="@color/list_tag_stroke"/>
        </shape>

    </item>

</layer-list>